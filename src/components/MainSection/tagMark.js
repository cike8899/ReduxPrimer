import style from 'styles/blogMainContent.less';

export default (props) => {
    return (
        <span className={style["article-mark"]}>
            <a>
                [javasctipt]
                            </a>
        </span>
    )
}